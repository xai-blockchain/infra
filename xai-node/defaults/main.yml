---
# Default variables for xai-node role

chain: xai
chain_id: xai-mvp-testnet-1
daemon_name: xai
daemon_user: xai
daemon_home: /home/xai/xai
venv_path: "{{ daemon_home }}/venv"

# XAI port range: 12xxx (to avoid conflicts with other chains)
# Actual ports are set per-node in inventory/testnet.yml
# These defaults are for local development only
rpc_port: 12545
p2p_port: 12333
ws_port: 8766

service_name: xai
