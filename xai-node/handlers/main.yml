---
# Handlers for xai-node role

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes

- name: restart xai service
  ansible.builtin.service:
    name: "{{ service_name }}"
    state: restarted
