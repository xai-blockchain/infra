# Managed by Ansible - DO NOT EDIT MANUALLY
# XAI testnet virtual hosts

# RPC endpoint
server {
    listen 80;
    listen 443 ssl http2;
    server_name testnet-rpc.xaiblockchain.com;

    ssl_certificate /etc/letsencrypt/live/testnet-rpc.xaiblockchain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/testnet-rpc.xaiblockchain.com/privkey.pem;

    location / {
        limit_req zone=api burst=20 nodelay;
        proxy_pass http://127.0.0.1:{{ rpc_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# REST API endpoint
server {
    listen 80;
    listen 443 ssl http2;
    server_name testnet-api.xaiblockchain.com;

    ssl_certificate /etc/letsencrypt/live/testnet-api.xaiblockchain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/testnet-api.xaiblockchain.com/privkey.pem;

    location / {
        limit_req zone=api burst=20 nodelay;
        proxy_pass http://127.0.0.1:{{ rpc_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# WebSocket endpoint
server {
    listen 80;
    listen 443 ssl http2;
    server_name testnet-ws.xaiblockchain.com;

    ssl_certificate /etc/letsencrypt/live/testnet-ws.xaiblockchain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/testnet-ws.xaiblockchain.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:{{ ws_port }};
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 86400;
    }
}

# Explorer
server {
    listen 80;
    listen 443 ssl http2;
    server_name testnet-explorer.xaiblockchain.com;

    ssl_certificate /etc/letsencrypt/live/testnet-explorer.xaiblockchain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/testnet-explorer.xaiblockchain.com/privkey.pem;

    location / {
        proxy_pass http://127.0.0.1:{{ explorer_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Faucet
server {
    listen 80;
    listen 443 ssl http2;
    server_name testnet-faucet.xaiblockchain.com;

    ssl_certificate /etc/letsencrypt/live/testnet-faucet.xaiblockchain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/testnet-faucet.xaiblockchain.com/privkey.pem;

    location / {
        limit_req zone=api burst=5 nodelay;
        proxy_pass http://127.0.0.1:{{ faucet_port }};
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
